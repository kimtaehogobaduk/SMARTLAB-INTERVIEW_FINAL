rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // SmartLab Interview Evaluator Firestore rules
    // Allow read and write for app operations across rooms, candidates, evaluations, audit_logs, settings
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
